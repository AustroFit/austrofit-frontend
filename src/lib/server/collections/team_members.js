export const teamCollection = {
  name: 'team_members',
  
  fields: [
    "id", "sort", "image", "visible", "first_name", "job_title", "state", "quote",
  ],
  
  defaultSort: 'sort',
  
  // Individual filter builders (composable)
  filterBuilders: {
  }
     
    
}
